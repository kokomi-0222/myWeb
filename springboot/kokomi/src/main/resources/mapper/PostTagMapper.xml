<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.kokomi.mapper.PostTagMapper">

    <select id="selectTagNamesByPostId" resultType="java.lang.String">
        SELECT tag_name FROM post_tag WHERE post_id = #{postId}
    </select>

    <insert id="insertBatch" parameterType="list">
        INSERT INTO post_tag (post_id, tag_name)
        VALUES
        <foreach collection="tags" item="tag" separator=",">
            (#{postId}, #{tag})
        </foreach>
    </insert>

    <delete id="deleteByPostId">
        DELETE FROM post_tag WHERE post_id = #{postId}
    </delete>

</mapper>
